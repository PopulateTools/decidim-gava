<div class="reveal" id="loginModal" data-reveal>
  <div class="reveal__header">
    <h3 class="reveal__title"><%= t(".please_sign_in") %></h3>
    <button class="close-button" data-close aria-label="Close modal"
      type="button">
      <span aria-hidden="true">&times;</span>
    </button>
  </div>

  <div class="row">
    <div class="columns medium-12 medium-centered">
      <p><%= t("authentication_required", scope: "decidim.devise.sessions.new") %></p>
    </div>

    <div class="columns medium-8 medium-centered">
      <%= decidim_form_for(Decidim::User.new, as: :user, url: session_path(:user), html: { class: "register-form new_user" }) do |f| %>
        <div class="actions">
          <%= f.submit t("sign_in", scope: "decidim.devise.sessions.new"), class: "button expanded" %>
        </div>
      <% end %>
    </div>
  </div>

</div>
